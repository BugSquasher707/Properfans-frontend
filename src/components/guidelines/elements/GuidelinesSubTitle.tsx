import React from "react"

const GuidelinesSubTitle = ({ text }: { text: JSX.Element }) => {
  return (
    <>
      <div className="mb-12 w-full text-18 font-bold text-black">{text}</div>
    </>
  )
}

export default GuidelinesSubTitle
